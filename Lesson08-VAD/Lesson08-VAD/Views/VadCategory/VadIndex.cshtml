@model IEnumerable<Lesson08_VAD.Models.VadCategory>

@{
    ViewBag.Title = "VadIndex";
}

<h2>VadIndex</h2>

<p>
    @Html.ActionLink("VAD: Create New", "VadCreate")
</p>
<table class="table table-bordered">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.VadCategoryName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.VadCategoryName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.VadCategoryId }) |
            @Html.ActionLink("Details", "Details", new { id=item.VadCategoryId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.VadCategoryId })
        </td>
    </tr>
}

</table>
